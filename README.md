# food_management_system-cpp
A console-based multi-role Food Management System (FMS) built in C++. Supports role-based login (Admin, Manager, Chef, Sales) with file-based data persistence for inventory, recipes, orders, and credentials. Features menu display, stock management, order processing, and invoice generation — all without databases or GUI frameworks.
